from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
    version="0.1.0",
    description="recreating the SPI predictions for any premier league game, in order to help forecast for unarranged fixtures in FPL",
    author="Jamie Barker",
    license="",
)
